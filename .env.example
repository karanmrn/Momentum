# Local demonstration runs without credentials.
PORT=4173
# Hosted demo requires an isolated PostgreSQL database and the demo migration.
# DATABASE_URL=
# Vercel uses HTTP Basic username demo and this secret password.
# DEMO_ACCESS_CODE=replace-with-at-least-16-random-characters
# Optional server-only source extraction. Never use a VITE_ prefix.
# CONTEXT_DEV_API_KEY=
